import notifySlack from 'busyffl/utils/notify-slack';
import { module, test } from 'qunit';

module('Unit | Utility | notify slack');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notifySlack();
  assert.ok(result);
});
