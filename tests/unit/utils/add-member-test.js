import addMember from 'busyffl/utils/add-member';
import { module, test } from 'qunit';

module('Unit | Utility | add member');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addMember();
  assert.ok(result);
});
