import loadAll from 'busyffl/utils/load-all';
import { module, test } from 'qunit';

module('Unit | Utility | load all');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loadAll();
  assert.ok(result);
});
