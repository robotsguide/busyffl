import setRoster from 'busyffl/utils/set-roster';
import { module, test } from 'qunit';

module('Unit | Utility | set roster');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setRoster();
  assert.ok(result);
});
