
//import addMember from 'busyffl/utils/add-member';

export function initialize(application) {
  window.BusyFFL = application;
 // window.addMember = addMember(application);
}

export default {
  name: 'application',
  initialize
};
