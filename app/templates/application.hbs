
<div class="v-application {{if session.isAuthenticated '' 'no-auth'}} {{if isSync 'syncing'}}">
  {{#if session.isAuthenticated}}
    <header>
     <span class="logo" {{action 'home'}}><b>busy</b>FFL</span>

     <span>
        <ul>
          <li {{action 'home'}}>Roster</li>
          <li {{action 'teams'}}>Teams</li>
          <li {{action 'players'}}>Players</li>
          <li {{action 'waivers'}}>Waivers</li>
          <li {{action 'trades'}}>Trade Feed</li>
          <li {{action 'settings'}}>Settings</li>
          <li {{action 'logout'}}>Logout</li>
        </ul>
     </span>
    </header>
  {{/if}}

  <div class="main-content">
    {{outlet}}
  </div>
</div>
